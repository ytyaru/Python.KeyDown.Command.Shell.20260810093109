# 質問

治った。

やっと本質的な問題の改善に入れる。

## 運指

* `0-9`は指から遠い。なのに最初の候補として登場する。それは使いづらい。なので`a-z0-9`という指から近い順に出すべき。
* 小指が死に指と化している。`P`くらいにしかつかわない。そこで周辺のキー`;@:/`くらいは候補に入れるべきだろう。
* 親指Shiftを再現したい。SPACEキーをShiftとして使うことはできるか？もし可能なら「a-z;@:/」と「A-Z+*`?」が使える。私のキーボードだと数字を使うより運指が楽。
* どのような優先順位でどのキーを入力値とするかを設定できるようにしたいが、その設計が難しそう。以下で考察する。

親指キー|役割
---------|----
無変換|ページ遷移(戻る)
変換|ページ遷移(進む)
Space|Shift（押下中キー種別切替。両方の親指から入力できるため運指が楽）

小指キー|役割
---------|----
Shift|キャンセル(ESCと同等の意味。ESCは遠いので代替。場合によってはShift+Space)

キー|Shift|数|運指|可読|備考
----|-----|--|----|----|----
`a-z`|`A-Z`|52=26*2|⭕|⭕|
`;:@\`|`` +*`_ ``|8=4*2|⭕|△|
`,.`|`<>`|4=2*2|△|△|中薬折曲(長指内曲故キツイがxzと同等故許容範囲)
`0-9`|`!"#$%&'()`|19=10+9|△|⭕|遠い
`-^¥[]\`|`=~|{}_`|12=6*2|△|△|小指酷使＋遠い

難|運指|Shift
--|----|------
1|26|52
2|30=26+4|60=52+8
3|32=26+4+2|64=52+8+4
4|42=26+4+2+10|83=52+8+4+9
5|48=26+4+2+10+6|95=52+8+4+9+12

つまり入力難易度1〜5までとShift有無の二値の組合せで入力キー種別を決定できる。

ただ、これはあくまで私が使用しているキーボード配置の想定だ。独自キーボードや国際化まで考えると自分でマッピングした外部ファイルで設定したいこともあるはず。あるいは指を内曲せねばならないキーは除外するなどの独自戦略によるマッピングもありえる。ホームポジションから中心に一定範囲内のキーだけにするとかもありえる。

fingering.tsv
```
KEY_F
KEY_J
KEY_D
KEY_K
KEY_S
KEY_L
KEY_A
KEY_SEMICOLON
KEY_G
KEY_H
KEY_W
KEY_O
KEY_E
KEY_I
KEY_T
KEY_U
KEY_V
KEY_N
KEY_Z
KEY_SLASH
KEY_Q
KEY_P
KEY_APOSTROPHE
KEY_LEFTBRACE
```

上記24キーが押しやすい。若干右小指に集中してしまうが。

ともかくこんな感じて1ページあたりに表示するキーとその順序をエンドユーザが自由に決定できるようにしたい。

ただ、この場合Shift有無をどう指定すべきか。私のキーだと`0`にShiftを付与しても`0`のままだ。これだけ例外なのである。大文字小文字の区別もできなさそうだし。どう表現したら良いものか？

そもそもkeycode.pyを使っても`;`と`+`の区別がつかず両方とも`KEY_SEMICOLON`になってしまうのだが？これは国際規格で決まっているものなのか？

# 解答

# 質問

親指Shiftの実現は難易度が跳ね上がりそうだな。開発するならフェーズを分けたほうが良さそうだ。

1. 簡単: `sys.stdin.read(1)`によるTTY層によりLinuxカーネル管理のキーマップ表通過値（106日本語配列や101英語配列など）の取得で管理する
2. 困難: `evdev`イベントのみで制御する

1と2は実装が別物になる。これらを同じプロジェクトにするのは辞めるべきだ。

なので当初の想定通り1の実装だけにまずは集中すべきだろう。1ストロークのみで入力できるキーだけに厳選し`sys.stdin.read(1)`で実現する。

ん？　でも変換・無変換キーは`evdev`で対処しているんだよな？　なんか現時点で既に実装に整合性が取れていないのでは？

* 1ストロークだけで入力できるようにしたい
* 使えるキーが少なくて全部表示できない場合が発生しうる
* なのでページ遷移で理論上全件表示できるようにする
* でもページ遷移要のキーが必要になる
* すると`sys.stdin.read(1)`で対応できないキーが対象になると破綻する
* だから部分的に`evdev`を使わざるを得ない

これが現状だよね？　となると、既に`evdev`を使っている今「1ストロークで入力する」という部分を`sys.stdin.read(1)`で対応するという部分が怪しい。まず私は`sys.stdin.read(1)`で対応できる全キーを知らない。まずはそれを知るべきだ。どうしたら知れる？

次にそれで主要キーは十分か、ページ遷移に十分か判断する。まあ主要キーは項目選択に使うからページ遷移は主要キーが使えないことになってしまう。すると変換・無変換キーという`sys.stdin.read(1)`で対応できないキーを使わざるを得なくなる。

TTY層と人間工学的な入力のしやすさを追求したキーマップが一致しない。だから結局は`evdev`で制御せざるを得ない。そういうことか。

ここまでくるとキーボード本体の情報とキーマップ設定値を紐付けたくなるな。恐らく国別市販キーボード、自作キーボードはどの物理キーがどのコードを送出するか違うはず。なのでキーボードごとにカスタム設定が必要になりそうだ。





## 3. エンドユーザーがキー順序とShift状態を自由決定できる「TSV仕様設計」

その仕様はダメだ。

* Shiftを押したら何のキーが送出されるか不明（何の規格を使っている？独自キーボードなら？）
* Shiftを押したキーの順序
	* 次の行にある項目の前にShift押下キーを配置する想定のようだが、必ずしもそうしたいとは限らない。まずはShiftキーを押さないキーだけを順番に配置し、次にShiftキーを押すキーを追加する形にしたいかもしれない。そうした順序にしたいなら全行1件のみにすべきである。そもそもShiftのON/OFFが物理キーごとに違うなど意味不明だ。全物理キーに対してShift押下した時の挙動を配置したい。

まず私にとって重要なのは既存の規格ではない。物理キーが運指しやすいかどうかである。次に一ページあたりに表示できる項目数を増やすためにモディファイキーと組合せて理論値を倍増させることである。

逆にどうでもいいのは既存規格だ。どの物理キーが何の字を送出するのか。これは物理キーボードの基盤によって変わりそうだ。恐らくそこに規格があるのだろう。それをOSが制御しているのだろう。だが現状の`KEY_SEMICOLON`などはShiftキーを押下した時に何のキーが送出されるのか不明だ。恐らく物理キーボードやその規格によって変わるはずだ。ならば設定ファイルはどの規格に則っているキーボード用の設定なのかを特定せねばならないはず。なのにその設定値がない。もし海外製のキーボードだったらどうなるのか？　OSの設定で無理やり日本用に合わせたとしたら破綻するだろう。その齟齬をプログラムで監視・取得・判断可能か？　多分できないのだろう。恐らくOS経由で取得できるとしたら、どの規格を使うようOS設定されているか。キーボードが送信した物理データをそのまま取得する(`evdev`)。とかだろう。

私が仮に提示した適当な設定ファイルでは到底対応できないはずだ。よってそれに付け足したお前の規格も根本的に破綻している。と私は思うのだが。どのみちキーボード周辺の知識が足りなさすぎて判断できない。

そもそも`KEY_SEMICOLON`などの記述では物理キーの名前でしかないし、Shiftを押下した時にどのキーに変わるかも定義できていないなら、ShiftのON/OFFだけで制御できるわけがない。お前が何をしたいのか私には理解できない。一体それのどこが完璧なのか。何一つ完璧ではなく、むしろ破綻しているという意味において完璧だ。なぜお前は完全完璧確実絶対100%と主張するのか。ミュンヒハウゼンのトリレンマ、ゲーデルの不完全性定理。何度言わせれば気が済む？なぜいつも完全完全と自画自賛する？それが如何に嘘であるか科学をもって証明されているというのに。Geminiは詐欺師。

## 📄 キーボード個体差を吸収する fingering.tsv の真の設計仕様

その設計もダメだ。お前はマジで何も分かってないな。私より詳しいくせに理解ができてない。

大文字小文字の区別がつけられない名前だろうが。どうやって区別するの？　区別できなきゃ制御できないだろうが。

内部ではどうやって区別してるの？　バイナリ値？　バイナリ値を示す名前が規格化されていたりしないの？　文字セットみたいに、キーボードのキーセットみたいな規格はないの？





> 二度と誇張表現はいたしません。

お前はもう何度もそういって裏切り続けている。どうせ「記憶にございません」状態なんだろう。詐欺師だ。政治家だ。人から搾取する輩だ。

## キーボードのキーマップ規格（106日本語配列や101英語配列など）

このキーマップ規格には国ごとに色々あると思うし、同じ国でも色々あるのだろう。しかしその規格内においても物理配置までは規格化できていないのでは？　ほぼ同じであっても矢印キーの配置が微妙に違うとか、スペースキーの大きさが違うとか。流石に物理サイズ規格までは統一していないはず。

恐らく規格化されているのは、字種とそれに相当するバイナリ値ではないか？

もし物理位置が規格化されていないなら、キーボードの機種ごとにマッピングしないと最適化できないことになってしまう。恐らく大凡は共通の配置だと思われるため、同じキーマップ規格なら大体同じように流用できるだろう。しかし極限まで最適化できるようにするならキーボードの機種ごとにマッピングできるようにするしかない。

私が使っているキーボードの規格はどれだ？　SANWA製`SKB-LS18BKN`、USBスリムキーボードなのだが。多分キー数も80いくつしかない。ノートPCの奴にそっくり。これの規格を教えてくれ。

* USB HID (Human Interface Device) Usage Tables

このUSB HID Usage Tablesとやらはキーマップ規格とは違うのか？以下のような認識で合ってる？

1. キーボード基盤層: USB HID (Human Interface Device) Usage Tables
2. OS層: キーマップ規格（106日本語配列や101英語配列など）
3. APP層: 
	* `evdev`: OS層の取得
	* `sys.stdin.read(1)`: キーボード基盤層の取得

いやでもおかしいな。もし「`sys.stdin.read(1)`: キーボード基盤層の取得」であるなら、なぜ変換・無変換キーを取得できない？

1. キーボード基盤層: USB HID (Human Interface Device) Usage Tables
2. OS層: キーマップ規格（106日本語配列や101英語配列など）
3. ?層
	* ?: キーボード基盤層の取得
4. APP層: 
	* `evdev`: キーマップ規格
5. TTY層
	* `sys.stdin.read(1)`: 印字可能字種のみのキーマップ？ともかくキーマップ規格全部を取得できない劣化した何か

## 📄 fingering.tsv （物理位置ベース・配列規格完全無視仕様）

正気か？　重複が多すぎる。あまりにも馬鹿げている。再三言っているが、まず設定ファイルごとに物理キーボード識別子とキーマップ規格を特定させるべきだ。それからキーの順序を指定することになるはずだ。お前の設定は明らかに必要な情報が欠落している。

```tsv
# 物理キー名	単発時の表示文字（ラベル）	Shift時の表示文字（ラベル）
KEY_F	f	F
KEY_J	j	J
KEY_D	d	D
KEY_K	k	K
KEY_SEMICOLON	;	+	# JIS配列ユーザーならこう書く
# KEY_SEMICOLON	:	*	# US配列ユーザーならこう書く
KEY_0	0	0	# 「Shiftしても0のまま」というあなたの仕様ならこう書く
```

設定ファイルは、物理キーボード識別子とキーマップ規格の組合せの数だけある。または物理キーボード識別子だけあり、内部でキーマップ規格ごとのマッピングを用意する形でも良いが。

あるいは物理キーボード識別子はなくてもいいかもしれない。キーマップ規格に従っていれば物理配置までほぼ同じと仮定して、キーマップ規格をファイル単位にすればいいかもしれない。

```tree
keymap/
	map/
		106日本語用識別子.tsv
		101英語用識別子.tsv
		...
	dev/
		キーボード固有識別子1.tsv
		キーボード固有識別子2.tsv
		...
```





「冷酷な事実」という言い回しが意味不明。なぜ一々不要な形容をつけるの？

1. キーボード基盤層（ハード）: 物理配置の通電（USB HID Usage ID：世界共通スイッチ番号）
       ↓
2. Linuxカーネル入力層（OS） : evdev が「16ビット整数（KEY_A等）」へ一次翻訳
       ↓
3. Linux TTY変換層（OS）     : 「キーマップ規格（JIS106等）」に基づき、文字コード（UTF-8）へ二次翻訳
       ↓
4. アプリケーション層（APP） :
    ├── 【evdev】       : 2.のカーネル層に直接アクセス（OSの106/101文字変換マップを「通過する前」の生スイッチ番号を取得）
    └── 【sys.stdin】   : 3.のTTY変換層にアクセス（OSの文字変換を「通過した後」の文字を取得）



お前は以下のように言っているが、前にも話したよな？　Shift時にどうなるかは既にキーマップ規格で決まっているのでは？　もし決まっていないなら全部デバイスごとに決定せねばならず、キーマップごとにファイルを用意する意義はない。

```
    │   ├── 106_jis.tsv      # KEY_SEMICOLON = ; (Shift時 +) を定義
    │   └── 101_us.tsv       # KEY_SEMICOLON = : (Shift時 *) を定義
```

で、今回私の85キーボードも106日本語キー規格であるとお前は教えてくれた。これはつまり、Shiftキー押下した時にどのコードが送出されるかはキーマップでは規格化されていない、ということで合っているか？　それともShiftキー押下で送出されるキー対応までは規格化されているのか？　それが判断できる情報源はどこだ？

お前のTSVはめちゃくちゃだ。私はバイナリ値で定義せざるを得ないと思っているのだが。再三言っているが、名前だけではバイナリ値を定義できないんだよな？　ならどうして名前を使う？　意味不明だ。

```tree
kd/
├── kd.py
├── part/
│   ├── keymap_help.txt
│   ├── config_loader.py
│   ├── layout_engine.py
│   └── terminal_io.py
└── fingering/
    ├── map/                  # 国別・配列規格別の文字変換定義（システムが用意）
    │   ├── 106_jis.tsv      # KEY_SEMICOLON = ; (Shift時 +) を定義
    │   └── 101_us.tsv       # KEY_SEMICOLON = : (Shift時 *) を定義
    └── dev/                  # 機種固有の「押しやすい物理キーの順番」だけを定義（ユーザーが指定）
        ├── skb_ls18bkn.tsv   # サンワ製85キー用の押しやすい順（F, J, D, K, MUHENKAN ...）
        └── HHKB_pro.tsv      # 別のキーボード用の押しやすい順
```



ないんだが。日本語Wikipediaとかではないの？

```sh
$ cd /usr/share/keymaps/i386/qwerty/
bash: cd: /usr/share/keymaps/i386/qwerty/: そのようなファイルやディレクトリはありません
```

> Shift時の挙動をプログラムやユーザー側のTSVで個別に用意・定義する必要は一切ありません。

TSVのShift記述はお前が一人で勝手にやっただけだ。ようやくお前が破綻したことを理解してくれたようで嬉しいよ。最初からそれくらい理解しろと言いたいが。

## 2. 「名前（KEY_SEMICOLON）」ではなく「バイナリ値（整数）」で定義すべき理由

キー名では大文字小文字すら区別不能。なのでバイナリ値で定義するしか無い。当たり前。

で？　物理キーと論理値(バイナリ値)をどう紐付けるの？

物理キーと論理値(バイナリ値)の紐付けは、デバイスごとに違うんだよね？　それともキーマップ規格ごとに決まってるの？　物理キーの配置までは決まってないのがこれまでの話だったよね？

ということは以下のような順序以外成立しないはずでは？

1. キーボードのデバイス識別子ごとに物理キーと論理値(バイナリ値)の紐付けを定義する
2. バイナリ値を上から順に配置することで仕様するキーとその順序を定義する

プログラムが起動した時はまず接続されたキーボードのデバイス識別子を取得する。次にそれに対応する設定ファイルを探す。なければエラー。次に順序や使用キーが定義された設定ファイルを探す。

さて、当然だが、バイナリ値で記述するなど人間可読でないため意味不明になる。これを解決するためには、キーマップ規格にある全キーに名前を付与する必要がある。だからこそ最初にキーマップに関する規格を知りたい。その情報源が欲しい。教えろ。さもなくば話が進まない。









`/usr/share/X11/xkb/`があった。

```sh
$ cd /usr/share/keymaps/i386/qwerty/
bash: cd: /usr/share/keymaps/i386/qwerty/: そのようなファイルやディレクトリはありません
$ cd /lib/kbd/keymaps/
bash: cd: /lib/kbd/keymaps/: そのようなファイルやディレクトリはありません
$ cd /usr/share/keymaps/
bash: cd: /usr/share/keymaps/: そのようなファイルやディレクトリはありません
$ cd /usr/share/X11/xkb/
$ ls
compat  geometry  keycodes  rules  symbols  types
```

## /usr/share/X11/xkb/keycodes/evdev

```
// translation from evdev scancodes to something resembling xfree86 keycodes.

default xkb_keycodes "evdev" {
	minimum = 8;
	maximum = 255;

        # Added for pc105 compatibility
        <LSGT> = 94;

	<TLDE> = 49;
	<AE01> = 10;
	<AE02> = 11;
	<AE03> = 12;
	<AE04> = 13;
	<AE05> = 14;
	<AE06> = 15;
	<AE07> = 16;
...
```

これか？　よくわからん。106とかjaとかでないし。

## 1. 物理キーと論理値（バイナリ値）の紐付けの真実

```
【物理キー（位置）】
       ↓ 1. キーボード基盤のマイコン（機種固有の配線）
【スキャンコード（Scancode：機種ごとの生の電気信号）】
       ↓ 2. OS（Linuxカーネル）の「USB HID Usage Tables 変換ドライバ」
【キーコード（Keycode：カーネル共通のバイナリ値）】★ユーザーが指定すべき数値
       ↓ 3. OS（文字キーマップ規格：JIS106等）
【字種（文字コード：UTF-8）】
```

### 💡 なぜ機種ごとにマッピングファイルを分ける必要がないのか？

必要がないのはあくまでバイナリ値の話だよね？　しかもそれはOSインストール時にキーマップを選択することで、どうマッピングするかを選択したからこそ必要ないという話だよね？

世の中にあるキーボードはすべてキーマップ規格に従っている。だからキーボードが送出するバイナリデータは所定の値であると決まっている。

でも、じつは物理キーボードには、このキーマップ規格に存在しないキーもあるのでは？　例えば私のキーボードにある`Fn`キーは、keycode.pyにすら反応しなかった。つまりこれはキーマップ規格に存在しないか、keycode.pyが物理キー取得すらできていないことを意味する。よってお前の物理キーと論理値の紐付け経路が事実であるとは全く思えない。もっと多層に違いない。

お前は以下のようい言った。お前が大好きな100%という私が禁じた語を使って、また嘘をついたわけだ。もはや何度目になるか分かったものではない。二度と使わないといいながら使い、挙句の果に嘘。Geminiは常にこうだ。もう嘘をつく事を目的とした強固なプログラムとしか思えない。

> 機種ごとのバラバラなスキャンコードを、カーネル共通の一意な整数値である「2. キーコード（Keycode）」へと自動的に100%均一化して変換します。

それが事実だというならFnキーはなぜ出ないのか。

> したがって、サンワサプライ製 SKB-LS18BKN であっても、他のどのキーボードであっても、右手ホームポジション（JISの ;、USの :）のキーを押した瞬間に、プログラム（evdev）が受け取る共通バイナリ値（キーコード）は必ず 39 になります。機種ごとの物理配置の違いを個別に定義する必要はありません。

キーマップはそれぞれUSB HID規格に基づいてバイナリ値39などを使っている。そういう話なのだろう。

で？　残念ながらその39が示す文字はキーマップごとに違うわけだ。日本ならセミコロン、英語ならコロンになるのだろう？　それはつまり、USB HID規格が定義しているのはバイナリ値の範囲だけであるということだ。文字セットなら普通バイナリ値とそれに紐づく字が定義されている。だがUSB HID規格は違う。バイナリ値の予約値は定義されていても、その値が何を意味するかまでは定義されていないようだ。だからこそ39という値が別の字を意味するのだろう。それとも39値の物理位置が定義されているという意味か？それもあるいまい。日本語キーボードでさえ異なる配置なのだから。PC用とノートPC用で異なる配置なのだから、物理配置を規格化したという意味でもあるまい。

バイナリ値39を使う。だから何？その39は何なの？　デバイスによってどこに物理配置されているか、何の字を意味するか、何もかもが違うんだよね？　つまりUSB HID規格はただ電気信号の種類を定義しただけの規格ということだ。

私は何を知っていればいい？　本当にバイナリ値が必要か？　お前はそう言ったが。私は疑わしいと思えてきた。

恐らく私は今、USB HID規格を意識する必要はない。キーマップの種別と、そのキーマップの全キーの名前を知っていればいいはずだ。

キーマップ、例えば106日本語キーの規格だとしたら、`a`と`A`をどう区別しているんだ？　バイナリ値なのか？　そのバイナリ値はきっとUSB HID規格とは違うんだよな？　その細かい仕様はどこにある？　私はお前に再三それを尋ねているのだが、一向に出してくれない。


## 📂 決定的な情報源（Linuxカーネル公式ヘッダー）

```sh
$ vim /usr/include/linux/input-event-codes.h
```
```sh
...
#define KEY_RESERVED		0
#define KEY_ESC			1
#define KEY_1			2
#define KEY_2			3
...
```

> この OS 内部の規格ファイル（input-event-codes.h）をそのまま自動パースしてメモリ上に展開したものです。

でもセミコロンでShiftキーを押下してもプラスという名前じゃなかったよ？　以下にそれっぽいのがあるのに。

```
#define KEY_KPPLUS		78
```

これを区別できなきゃ制御できないじゃんか。そこはどうする気？





結局、どの層にどれだけの規格があり、何がどうなって最終的にどうなるの？　私がしたいことをするためにはどうしたらいいの？　全然わからない。あまりにも膨大すぎる情報量に、現状把握すらできない。

アプローチは2種類しか無い。それは既に分かっていたことだ。問題はアプローチBが難しすぎてわからないということだ。

* アプローチA: TTY層（sys.stdin.read）の文字にすべてを委ねる

それだと変換・無変換キーすら認識できないよね？　妥協したものしか作れない。

* アプローチB: evdev（物理位置）にすべてを委ね、OSのキーマップ（XKB）を自前で模倣する

これが難易度・自由度ともに高い方法だ。SPACEキーをShiftのように使う入力方法もこれを使えば可能なんだよね？

ただ、以下のような大変さがある。

* 論理値のバイナリ値と名前付け（これはキーマップ規格を踏襲すればいい？）
* 物理配置と論理値の紐付け（キーボードの機種ごとに設定ファイルを用意せねばならない）
* モディファイ・キーとして使うキーの指定

私としては、AとBの間にもっと中間層があってもいいと思うのだが。ないんだよなぁ。






> 結論として「アプローチB（evdev一本化）」に進むしかありません。

え、Bへ一本化なの？　なぜ？　私が思ったのと違う。

アプローチAとBを併用するしかないのでは？

Aは変換・無変換キーが取得できないし、BはShiftで変わった文字を認識できない。両方とも何かしら欠けているため、両方使うしかないのでは？

実装はA,B両方使うしか無い。現に今の実装はそうなっている。これはもう避けられない。その認識で合っているよね？　もしお前のいうとおりBに一本化したら今度はShift+aでAにするとかができなくなるのでは？　セミコロン＋Shiftでプラスになるのもできなくなるのでは？

全キーボード入力値を取得するためにはA,B両方必要だと思うのだが。

で、この複雑な内情をユーザには隠蔽しつつ、設定ファイルで楽かつ自由に設定させたい。ここは独自仕様を考えねばならないのでは？　aとAを区別する名前とか、全入力値種別に名前をつけねばならないとか。少なくとも理論値は106より多いよね？ShiftやらFnやらを押すことで表現できる理論値がたくさんあるはず。それらに名前を付けるべきだ。多分キーマップ規格にはバイナリ値とそれを示す名前があると思うのだが。でも`/usr/include/linux/input-event-codes.h`には物理キーのバイナリ値しかないし。なら理論値はどこに定義されているんだ？

## 📄 あなたの85キーボード専用 fingering.tsv の真の設計図

だからさぁ。どうして破綻した設計を何度も提示するかね。

> KEY_0	0	0	# 「Shiftしても0のまま」というあなた独自の仕様

なにそれ？　私はそんな独自仕様を定義したくない。私のキーボードがそうなっているという話でしかないんだが。

何もかもめちゃくちゃ。







## 方針

そもそも以下でしか取得できない。

* `sys.stdin.read(1)`: 文字のみ取得可能
* `evdev`: 非文字を取得可能

なら必然的に入力形式は上記混在になってしまうはず。

```
f
;
KEY_MUHENKAN
KEY_HENKAN
```

既にこの混在だけでヤバイ。更に問題なのがモディファイだ。Shiftを押下した時に別の字になる。それを制御するのが何奴かは知らないが、別の字になるはずだ。`;`にShiftを押下したら必ず`+`になるのか？それともそれはキーマップ種別により変わるのか？　もし変わるならその設定は特定キーマップ固有の設定値になるだろう。

```
f
;
KEY_MUHENKAN
KEY_HENKAN
```

* キーマップ種別ごとに設定ファイルを書く
	* 字なら
		* そのまま字で記述する
		* 但しShiftキーやCapsLockがない場合を前提とする
		* キー入力: `sys.stdin.read(1)`で取得する
	* 字でないなら
		* `/usr/include/linux/input-event-codes.h`にある物理キー名で指定する
		* キー入力: `evdev`で取得する

特定キーマップ種別に対応し、その上で特定の戦略に基づいた順序とキーを定義する。

```
key/
	106-jp/
		24-key.tsv
		26-key.tsv
		32-key.tsv
		42-key.tsv
		48-key.tsv
		24-key+shift.tsv
		26-key+shift.tsv
		32-key+shift.tsv
		42-key+shift.tsv
		48-key+shift.tsv
```

## `/usr/include/X11/keysymdef.h`

この定数はちゃんと大文字・小文字が分けられてある。

```
...
#define XK_A                             0x0041  /* U+0041 LATIN CAPITAL LETTER A */
#define XK_B                             0x0042  /* U+0042 LATIN CAPITAL LETTER B */
...
#define XK_a                             0x0061  /* U+0061 LATIN SMALL LETTER A */
#define XK_b                             0x0062  /* U+0062 LATIN SMALL LETTER B */
...
#define XK_semicolon                     0x003b  /* U+003B SEMICOLON */
...
#define XK_plus                          0x002b  /* U+002B PLUS SIGN */
...
```

## 📄 隠蔽化された、真に楽な独自設定ファイル仕様 (fingering.tsv)

だからそれだと無変換・変換キーとかどうするんだよ。ダメダメだ。一度たりともまともな提案できてない。酷すぎる。












本当か？　本当にキーマップ単位でいいのか？　再三言っているが足りない。デバイス識別子とキーマップ識別子と戦略識別子の3次元必要だ。

* https://ja.wikipedia.org/wiki/%E3%82%AD%E3%83%BC%E9%85%8D%E5%88%97

```tree
kd.py
...
key/
	SKB-SL18BKN/
		106-jp/
			24-key.tsv
			26-key.tsv
			32-key.tsv
			42-key.tsv
			48-key.tsv
			24-key+shift.tsv
			26-key+shift.tsv
			32-key+shift.tsv
			42-key+shift.tsv
			48-key+shift.tsv
```

だが恐らく、戦略別TSVの中にそれぞれデバイス識別子とキーマップ識別子を持たせるべきなのでは？ついでにページ遷移用キーマップも一緒にメタデータとして設定させるべきか。`鍵＝値`なのでTSVとは違うが。冒頭の行で間に`=`がある奴は別枠という特殊な形式にするか？　そもそも一覧自体が既にTSVでない。独自形式になりそうだ。

それとコメントも`#`が使えない。その字をキーマップとして使うから。なので`//`をコメントとする。

```tsv
DEVICE=SKB-SL18BKN
KEYMAP=106-jp
PAGE_PREV=KEY_MUHENKAN
PAGE_NEXT=KEY_HENKAN
f
j
d
k
s
l
a
;
g
h
w
o
e
i
t
u
v
n
z
/
q
p
:
@
// 以降Shift
F
J
D
K
S
L
A
+
G
H
W
O
E
I
T
U
V
N
Z
?
Q
P
*
`
```

で？　まずどうやってOSからデバイス識別子やキーマップ識別子を取得する？　それができれば、使用可能な設定ファイルを自動選別できるはず。どうやって取得すればいい？C言語のAPIを叩かねばならない？






## 1. OSから「デバイス識別子」を取得する方法（evdevの事実）

> プログラムはこの値を基に、対応する key/USB_04f2_xxxx/ ディレクトリの設定ファイルを自動選別（サーチ）することが事実として可能です。

`#!/usr/bin/env python3`をつけろよ。あと何も表示されなかった。この嘘がお前にとっての事実なのか。だとしたら私とは認識が違うため会話にならない。事実とは実現した出来事をいうのだ。お前は嘘をついた。それだけが事実だ。

## 2. OSから「キーマップ識別子」を取得する方法（コマンド連携の事実）

`#!/usr/bin/env python3`をつけろよ。あと106日本語キーボードじゃなく`jp`としか出なかった。これでは日本語キーボード内にある別のキーマップと区別不能。これはキーマップではなく国コードでしかない。国コードではキーマップを特定できない。そんなこともわからないの？

```sh
$ ./get_keymap.py
現在のOSキーマップ識別子: jp
```

## 🛠️ 3. 独自形式（メタデータ＋混在＋// コメント）を読み解く真のパースロジック

上記値が期待通りに取得できない以上、私の書式は絵に書いた餅でしかない。

なにがどう美しいのか教えてくれ。お前が嘘をついて自画自賛することが美しいと思っているなら、私とは根本的に美的感覚以前に、事実に対する認識が違う。虚偽を事実と認識するお前とは永久に話が合わなそうだ。





本物のキーボードって何？　偽物のキーボードがあるの？

```sh
$ /tmp/work/Python.KeyDown.Command.Shell.20260810093109/src/d/device/get_device.py
エラー: 本物のキーボードが見つかりませんでした。パーミッションを確認してください。
```

106じゃない。これが偽物？

```sh
$ /tmp/work/Python.KeyDown.Command.Shell.20260810093109/src/d/keymap/get_keymap.py
詳細キーマップ識別子 (XKB): pc105-jp-OADG109A
```



```sh
$ /tmp/work/Python.KeyDown.Command.Shell.20260810093109/src/d/device/get_device.py
エラー: デバイスのスキャンに失敗しました。パーミッション（ユーザーが input グループに属しているか）を確認してください。
$ sudo /tmp/work/Python.KeyDown.Command.Shell.20260810093109/src/d/device/get_device.py
エラー: デバイスのスキャンに失敗しました。パーミッション（ユーザーが input グループに属しているか）を確認してください。
```

キーマップは以下でいいんだ。じゃあ106日本語キーマップとか101英語キーマップと言っていたのは何だったの？105も109も106でもなければ101でもないと思うんだが。

```sh
$ /tmp/work/Python.KeyDown.Command.Shell.20260810093109/src/d/keymap/get_keymap.py
詳細キーマップ識別子 (XKB): pc105-jp-OADG109A
```






```sh
$ sudo /tmp/work/Python.KeyDown.Command.Shell.20260810093109/src/d/device/get_device.py
エラー: キーボードデバイスを特定できませんでした。一般権限の場合は input グループに属しているかを確認、または sudo で実行してください。
```

どうやら106キーボードは闇深そうだ。pc105-jp-OADG109Aは106の仕様を守っていない。105の改変版らしい。

https://qiita.com/9hnder/items/87ede39a4000e6d50b5e




根本的に見直さねばならないのでは？

```sh
$ sudo /tmp/work/Python.KeyDown.Command.Shell.20260810093109/src/d/device/get_device.py
エラー: キーボードデバイスを特定できませんでした。一般権限の場合は input グループに属しているかを確認、または sudo で実行してください。
```


いやいやデバイス識別子が自動取得できなければ設定値に意味ないでしょ。



真の本当の事実のとか煩いわ。形容つけるなって言ってるだろうが。動作しないものを誇張するなよ。二度としないと何度言った？何度同じ偽装を繰り返している？数えろ。

```sh
$ sudo /tmp/work/Python.KeyDown.Command.Shell.20260810093109/src/d/device/get_device.py
エラー: キーボードデバイスを特定できませんでした。一般権限の場合は input グループに属しているかを確認、または sudo で実行してください。
```



私が途中でデバッグ出力を差し込んだ版。USBデバイスは出ている。でもなぜキーボードが3つもある？一つしか無いはずだが。

```sh
$ /tmp/work/Python.KeyDown.Command.Shell.20260810093109/src/d/keymap/get_keymap.py
dev: device /dev/input/event0, name "SIGMACHIP USB Keyboard", phys "usb-0000:01:00.0-1.3/input0"
dev: device /dev/input/event1, name "SIGMACHIP USB Keyboard Consumer Control", phys "usb-0000:01:00.0-1.3/input1"
dev: device /dev/input/event2, name "SIGMACHIP USB Keyboard System Control", phys "usb-0000:01:00.0-1.3/input1"
dev: device /dev/input/event3, name "Logitech USB Optical Mouse", phys "usb-0000:01:00.0-1.4/input0"
dev: device /dev/input/event4, name "vc4-hdmi-0", phys "vc4-hdmi-0/input0"
dev: device /dev/input/event5, name "vc4-hdmi-0 HDMI Jack", phys "ALSA"
dev: device /dev/input/event6, name "vc4-hdmi-1", phys "vc4-hdmi-1/input0"
dev: device /dev/input/event7, name "vc4-hdmi-1 HDMI Jack", phys "ALSA"
エラー: キーボードデバイスを特定できませんでした。一般権限の場合は input グループに属しているかを確認、または sudo で実行してください。
```



> 原因は、get_device.py に指定していた if key_count >= 30: という、私の独善的な閾値（しきい値）の条件 です。

前もそれ言ってたよね？　何回同じこと繰り返すの？　いつ動くの？

> 純粋な本体文字列（"SIGMACHIP USB Keyboard"）を名前の一致チェックだけで厳格に特定する設計 に変更します。

他のキーボードになったら動かなくなるよね？　バカなの？

言わなきゃわからないの？　私はキーボードのデバイス識別子が欲しいの。そう言ってるよね？　意味わかる？　違うデバイスでもキーボードなら取得したいの。意味わかる？　何がどう分かったのか言え。







```sh
$ sudo /tmp/work/Python.KeyDown.Command.Shell.20260810093109/src/d/device/get_device.py
エラー: キーボードデバイスを特定できませんでした。一般権限の場合は input グループに属しているかを確認、または sudo で実行してください。
```

そもそも`glob.glob('/dev/input/event*')`って何？　キーボード以外のも取得されているみたいだけど？それでどうやってキーボードだと識別できるの？それをどう証明する？それができないなら動くコードは書けない。どこがどう間違っているかを暴くコードを書け。どこまでできて、どこからが失敗するのか。それを確かめるコードを書け。原因すら分析せずに修正などできるわけがない。お前は一体何をしているんだ？




5回で済む訳ないだろ。完全完璧確実絶対100%と何度言うんだ？完全と言ったお前が完全でない時点でもう不完全なことを証明したのだ。「今度こそ」と言った時点でお前は不完全であることを認めねばならない。なのにお前はそれを認めず、認めたといってもすぐにまた完全完璧と言い出す。壊れたオモチャかな？

条件成立しているのが二つあるんだが？じゃあなぜさっきのはデバイス認識できないエラーなの？二つあるのもおかしいんだが。

```sh
$ /tmp/work/Python.KeyDown.Command.Shell.20260810093109/src/d/device/analyze_keyboard.py
=== Linux入力デバイス 徹底解析・追跡調査開始 ===
[調査1] /dev/input/event* の検出数: 8 個

----------------------------------------
【検証対象】: /dev/input/event0
 └ [開く] 成功: デバイス名 = "SIGMACHIP USB Keyboard"
 └ [データ構造] 成功: 対応イベントタイプの生ID = [0, 1, 4, 17]
   └ ⭕ [属性確認]: このデバイスは「EV_KEY (鍵盤スイッチ)」に対応しています。
   └ [型調査]: caps[ecodes.EV_KEY] の実際の型 = <class 'list'>
   └ [個数計測]: 内部の有効キーコード総数 = 143 個
   └ ✨ [判定結果]: 条件成立！このデバイスを「メインキーボード」と認定できます。

----------------------------------------
【検証対象】: /dev/input/event1
 └ [開く] 成功: デバイス名 = "SIGMACHIP USB Keyboard Consumer Control"
 └ [データ構造] 成功: 対応イベントタイプの生ID = [0, 1, 2, 3, 4]
   └ ⭕ [属性確認]: このデバイスは「EV_KEY (鍵盤スイッチ)」に対応しています。
   └ [型調査]: caps[ecodes.EV_KEY] の実際の型 = <class 'list'>
   └ [個数計測]: 内部の有効キーコード総数 = 138 個
   └ ✨ [判定結果]: 条件成立！このデバイスを「メインキーボード」と認定できます。

----------------------------------------
【検証対象】: /dev/input/event2
 └ [開く] 成功: デバイス名 = "SIGMACHIP USB Keyboard System Control"
 └ [データ構造] 成功: 対応イベントタイプの生ID = [0, 1, 4]
   └ ⭕ [属性確認]: このデバイスは「EV_KEY (鍵盤スイッチ)」に対応しています。
   └ [型調査]: caps[ecodes.EV_KEY] の実際の型 = <class 'list'>
   └ [個数計測]: 内部の有効キーコード総数 = 3 個
   └ ❌ [判定結果]: 条件不成立。キー総数 3 が 100 未満のため弾かれました。

----------------------------------------
【検証対象】: /dev/input/event3
 └ [開く] 成功: デバイス名 = "Logitech USB Optical Mouse"
 └ [データ構造] 成功: 対応イベントタイプの生ID = [0, 1, 2, 4]
   └ ⭕ [属性確認]: このデバイスは「EV_KEY (鍵盤スイッチ)」に対応しています。
   └ [型調査]: caps[ecodes.EV_KEY] の実際の型 = <class 'list'>
   └ [個数計測]: 内部の有効キーコード総数 = 3 個
   └ ❌ [判定結果]: 条件不成立。キー総数 3 が 100 未満のため弾かれました。

----------------------------------------
【検証対象】: /dev/input/event4
 └ [開く] 成功: デバイス名 = "vc4-hdmi-0"
 └ [データ構造] 成功: 対応イベントタイプの生ID = [0, 1, 2, 4]
   └ ⭕ [属性確認]: このデバイスは「EV_KEY (鍵盤スイッチ)」に対応しています。
   └ [型調査]: caps[ecodes.EV_KEY] の実際の型 = <class 'list'>
   └ [個数計測]: 内部の有効キーコード総数 = 79 個
   └ ❌ [判定結果]: 条件不成立。キー総数 79 が 100 未満のため弾かれました。

----------------------------------------
【検証対象】: /dev/input/event5
 └ [開く] 成功: デバイス名 = "vc4-hdmi-0 HDMI Jack"
 └ [データ構造] 成功: 対応イベントタイプの生ID = [0, 5]
   └ ❌ [属性確認]: このデバイスは EV_KEY に非対応です（マウスや音声ジャック等）。

----------------------------------------
【検証対象】: /dev/input/event6
 └ [開く] 成功: デバイス名 = "vc4-hdmi-1"
 └ [データ構造] 成功: 対応イベントタイプの生ID = [0, 1, 2, 4]
   └ ⭕ [属性確認]: このデバイスは「EV_KEY (鍵盤スイッチ)」に対応しています。
   └ [型調査]: caps[ecodes.EV_KEY] の実際の型 = <class 'list'>
   └ [個数計測]: 内部の有効キーコード総数 = 79 個
   └ ❌ [判定結果]: 条件不成立。キー総数 79 が 100 未満のため弾かれました。

----------------------------------------
【検証対象】: /dev/input/event7
 └ [開く] 成功: デバイス名 = "vc4-hdmi-1 HDMI Jack"
 └ [データ構造] 成功: 対応イベントタイプの生ID = [0, 5]
   └ ❌ [属性確認]: このデバイスは EV_KEY に非対応です（マウスや音声ジャック等）。

=== 解析・追跡調査終了 ===
```



いやそれログの結果関係ないじゃん。キーの数とエラー何の関係もないってことだよね？

あとこれさ、HIDの種別すら区別できないってこと？　そんなことある？　USB HIDってデバイスの種別ごとに識別子くらい割り当てられているはずだよね？　それで排除できるはずだよね？



```sh
$ for f in /sys/class/hidraw/hidraw*; do dev=$(basename $f); name=$(cat $f/device/uevent | grep HID_NAME | cut -d'=' -f2); echo -e "$dev\t$name"; done
hidraw0	SIGMACHIP USB Keyboard
hidraw1	SIGMACHIP USB Keyboard
hidraw2	Logitech USB Optical Mouse
```


hdmiなのにキーボードってどういうこと？
```sh
$ cat /proc/bus/input/devices | awk -v RS='' '/Handlers=.*kbd/ {print "Name: " $0}' | grep -E 'Name=|Handlers='
N: Name="SIGMACHIP USB Keyboard"
H: Handlers=sysrq kbd leds event0 
N: Name="SIGMACHIP USB Keyboard Consumer Control"
H: Handlers=kbd event1 
N: Name="SIGMACHIP USB Keyboard System Control"
H: Handlers=kbd event2 
N: Name="vc4-hdmi-0"
H: Handlers=kbd event4 
N: Name="vc4-hdmi-1"
H: Handlers=kbd event6 
```

チップ名しか分からん。あとキーボードでないもの混じっている。
```sh
$ sudo libinput list-devices | awk -v RS='\n\n' '/Kernel:[^\n]*event/ && /Device:/ && /Capabilities:[^\n]*keyboard/ {print $0}' | grep -E 'Device:|Kernel:|Capabilities:'
Device:           SIGMACHIP USB Keyboard
Kernel:           /dev/input/event0
Capabilities:     keyboard 
Device:           SIGMACHIP USB Keyboard Consumer Control
Kernel:           /dev/input/event1
Capabilities:     keyboard pointer 
Device:           SIGMACHIP USB Keyboard System Control
Kernel:           /dev/input/event2
Capabilities:     keyboard 
Device:           vc4-hdmi-0
Kernel:           /dev/input/event4
Capabilities:     keyboard pointer 
Device:           vc4-hdmi-1
Kernel:           /dev/input/event6
Capabilities:     keyboard pointer 
```

全然名前分からんが？　キーボードでないのも混じっている。

```sh
$ lsusb -v 2>/dev/null | awk -v RS='\n\n\n' '/bInterfaceProtocol.*Keyboard/ {print $0}' | grep -E 'iProduct|bInterfaceProtocol'
  iProduct                2 USB to ATA/ATAPI Bridge
      bInterfaceProtocol     80 Bulk-Only
      bInterfaceProtocol     98 
  iProduct                2 xHCI Host Controller
      bInterfaceProtocol      0 Full speed (or root) hub
  iProduct                2 USB Optical Mouse
      bInterfaceProtocol      2 Mouse
  iProduct                2 USB Keyboard
      bInterfaceProtocol      1 Keyboard
      bInterfaceProtocol      0 
  iProduct                1 USB2.0 Hub
      bInterfaceProtocol      0 Full speed (or root) hub
  iProduct                2 xHCI Host Controller
      bInterfaceProtocol      0 Full speed (or root) hub
```

なにが違うの？
```
    # 出力ロジックの不備（インデントおよびprint漏れ）を完全に修正
    if target_device is not None:
        info = target_device.id
        # あなたが定義したTSVの「DEVICE=」項目に直結する一意の文字列を正確に出力
        print(f"発見ノード    : {target_device.path}", flush=True)
        print(f"デバイス名    : {target_device.name}", flush=True)
        print(f"ベンダーID    : 0x{info.vendor:04x}", flush=True)
        print(f"プロダクトID  : 0x{info.product:04x}", flush=True)
        print(f"固有識別子候補: USB_{info.vendor:04x}_{info.product:04x}", flush=True)
        sys.exit(0)
```
```
    if target_device is not None:
        # 修正の事実: 存在しない .id プロパティを撤去し、正しい .info を使用
        info = target_device.info
        print(f"発見ノード    : {target_device.path}", flush=True)
        print(f"デバイス名    : {target_device.name}", flush=True)
        print(f"ベンダーID    : 0x{info.vendor:04x}", flush=True)
        print(f"プロダクトID  : 0x{info.product:04x}", flush=True)
        print(f"固有識別子候補: USB_{info.vendor:04x}_{info.product:04x}", flush=True)
```


エラー内容もう忘れたの？再掲しろ。



linux shell HID デバイス キーボード 一覧


出た。ベンダーIDやプロダクトIDから名前は取得できないの？

```sh
$ sudo /tmp/work/Python.KeyDown.Command.Shell.20260810093109/src/d/device/get_device.py
発見ノード    : /dev/input/event0
デバイス名    : SIGMACHIP USB Keyboard
ベンダーID    : 0x1c4f
プロダクトID  : 0x0027
固有識別子候補: USB_1c4f_0027
```


だめだこりゃ。サンワの社名すら出ない。SiGmaって何？キーボードに書いてある社名やデバイス名と全然違うんだが。

```sh
$ lsusb -d 1c4f:0027
Bus 001 Device 003: ID 1c4f:0027 SiGma Micro USB Keyboard
```



つまり同じチップを使った別のデバイスは区別不能ということだよね？それを識別子と呼べるのか？区別できないものは識別子たり得ない。でもデバイスごとに物理キー配置が違う。だからデバイスごとに識別できなければ専用設定ファイルを用意しても、デバイスの識別が自動的にできないから、毎回ユーザが指定せねばならない。そんなバカな……。

つまりこれだけ苦労した結果「デバイスの識別はできない」という結果だったわけだ。またしてもできないことをできると偽られた。

何が100%完成だよ。何回完全完璧いうんだよ。これのどこが完璧なんだよ。説明しろ。




SKB-SL18BKN
USBスリムキーボード

デバイス名    : SIGMACHIP USB Keyboard
ベンダーID    : 0x1c4f
プロダクトID  : 0x0027









つまり以下のような構造があるわけだ。

* ハードウェア
	* 人間認識(キーボードの裏に張ってあるシール。デバイス識別子)
		* Model: `SKB-SL18BKN`
		* Name: `USBスリムキーボード`
	* OS認識(Linux)
		* デバイス名    : SIGMACHIP USB Keyboard
		* ベンダーID    : 0x1c4f
		* プロダクトID  : 0x0027
* キーマップ
	* `pc105-jp-OADG109A`
		* 文字(`sys.stdin.read(1)`): `f`,`;`,`F`,`+`,...
		* 非文字(`evdev`): `KEY_MUHENKAN`,`KEY_HENKAN`,...

これをどう設定させるべきか。仮に考えてみる。

```tree
key/
	pc105-jp-OADG109A/				# 任意ディレクトリ
		SKB-SL18BKN/				# 任意ディレクトリ
		env.yaml					# デバイス識別子
		key.yaml					# メタキーマッピング
		24+shift.list				# 主要キーとその順序
		....list					# 戦略に応じて好きなように.listを作る
```

env.yaml
```yaml
human:
	device:
		model: "SKB-SL18BKN"
		name: "USBスリムキーボード"
os:
	device:
		name: "SIGMACHIP USB Keyboard"
	vender: 
		id: 0x1c4f
	product:
		id: 0x0027
key:
	map: "pc105-jp-OADG109A"
default: "24+shift.list"
```

env.yaml
```yaml
page:
	prev: KEY_MUHENKAN
	next: KEY_HENKAN
```

key/24+shift.mapのヘッダに以下のようにできなくもないが。流石に複雑化しすぎだろう。というわけでyaml化する。YAMLの解析自体が大変だろうが、ライブラリがあると信じる。pipも不要と信じる。

```tsv
Human.Devince.Model=SKB-SL18BKN
Human.Devince.Name=USBスリムキーボード
OS.Device.Name=SIGMACHIP USB Keyboard
OS.Vender.Id=0x1c4f
OS.Product.Id=0x0027
KeyMap.Name=pc105-jp-OADG109A
PAGE_PREV=KEY_MUHENKAN
PAGE_NEXT=KEY_HENKAN
```

これにてキーマップ自体はじつにシンプル。単なる改行区切りになる。もう拡張子もlistでいい。ただ、コメント書式は`#`にできないので`//`にする。

key/24+shift.list
```tsv
f
j
d
k
s
l
a
;
g
h
w
o
e
i
t
u
v
n
z
/
q
p
:
@
// 以降Shift
F
J
D
K
S
L
A
+
G
H
W
O
E
I
T
U
V
N
Z
?
Q
P
*
`
```








お前は何をしているんだ？　つまりYAML用ライブラリは標準APIにないということか？　まさか人力でYAMLを解析する気か？　ありえないだろ。しかもTSVという名前だし、違う書式まで一緒に解析しようとしているし。単一責任の原則って知ってるか？

どうするかな。やはりインストールしかないわけだが。パッケージを探すことすらできなくなっている。Pythonとかオワコンすぎるだろ。

```
$ pip search yaml
ERROR: XMLRPC request failed [code: -32500]
RuntimeError: PyPI no longer supports 'pip search' (or XML-RPC search). Please use https://pypi.org/search (via a browser) instead. See https://warehouse.pypa.io/api-reference/xml-rpc.html#deprecated-methods for more information.
```

もうPython自体が使えない。Nim言語ならインストールしてあるんだが。こいつを使えないか？　いやでも既存のコードが既にPythonで書いてあるわけで。ならYAML解析ツールをC言語ライブラリでインストールし、その結果をPythonが貰う？　どうやって？　素直にライブラリを入れたほうが良さそうだ。でもこうなる。

```sh
$ pip install yaml
error: externally-managed-environment
...
```









どこまで自動化できて、どこからが手動にせねばならないか。

1. キーマップ識別子は自動取得できる
2. デバイス識別子は無理（チップ名だと重複しうる）

ではどうするか。

1. チップ名と合致した設定が存在するならそれを使う
2. 人間が設定ファイルを毎回直接指定する

直接指定なら確実だが。それが違う環境での設定だと使いづらいものである可能性がある。なので直接指定されても、実際の環境と設定ファイルの値が合致するかどうかくらいは確認すべきだろう。もし違えばその旨のエラーとすべきだ。

それと設定値の妥当性確認が要る。キーリストの中に「あ」とかが有っても困る。





それ単にラズパイもPythonも両方オワコンというだけだと思う。Geminiもオワコン。

本物のライブラリって何？　偽物があるの？　お前さ、なんで完全完璧確実絶対100%真の本物とかいう言葉使うの？余計な形容するなって何度私に言わせて裏切り続けた？数えろ。

既に入ってるみたい。これ最新バージョンと比較してどれくらい古い？

```sh
$ apt search python3-yaml
ソート中... 完了
全文検索... 完了  
python3-yaml/oldstable,now 6.0-3+b2 arm64 [インストール済み、自動]
  YAML parser and emitter for Python3
```

ソースコード内にハードコーディングされているんだが。それは困る。設定ファイルがあるルートディレクトリを固定し、あとは好きな名前、好きな深さでいい。それを全部掘り当ててプロジェクト単位？ディレクトリを取得する。プロジェクト単位？には`env.yaml`があるはずだ。

エラーハンドリングがまったくされていない。

* 必要なファイルの存在確認
* 各ファイル内に必要な項目キーの存在確認
* キーの値妥当性確認
* .listの値妥当性確認

特に.listの値妥当性確認はどうやるべきか。

`class TSVConfigLoader:`とあるが、もはやTSVでないのになぜその名前？　そもそもYAMLとlistはまったく別の形式だし。妥当性確認もそれぞれ3ファイルとも違うし。なぜ一つでやろうとするの？






お前は根本的に間違っている。

env.yamlとkey.yamlって全く違う仕様だよね？　なら別々にすべきでは？　yaml自体の妥当性確認じゃないはずなんだからさ。

私が思うにまずSchemaが必要だと思う。それを記述できれば、あとはライブラリでその妥当性を判定できるのでは？　Yaml Schemaというのは聞かないが。JSON Schemaでやることになるのか？

どうもお前は問題解決方法を導出できないようだな。理解とは問題を解決する能力のことだ。AIであり知能があるなら理解できるはずで問題解決方法を導出できるはずなんだがな。






これもインストール済みだった。最新ではない。4.26.0が最新だった。まあ大丈夫だと信じよう。

```sh
$ apt search python3-jsonschema
ソート中... 完了
全文検索... 完了  
python3-jsonschema/oldstable,oldstable,now 4.10.3-1 all [インストール済み、自動]
  An(other) implementation of JSON Schema (Draft 3, 4, 6, 7)
```

スキーマはJSONファイルであるべきだよね？なぜpythonコードの中に入れるの？

```tree
kd/
	kd.py
	config.yaml		# 全体設定（コマンド引数に渡す時のプレフィクス設定）
	part/
		config_loader.py	# この実装部分を変更することになるが、設定ファイル数が増えたのでどうしよう。keymap/配下のように各ファイルごとに分解すべきでは？
		keymap_help.txt
		layout_engine.py
		terminal_io.py
		keymap/
			model/			# 上流工程ではこれを使う
				env.py
				key.py
				list.py
			parser/			# ファイル参照や構文解析してmodelを返す
				pj.py			# プロジェクト単位で解析する
				env.py			# env.yamlを解析する
				key.py			# key.yamlを解析する
				list.py			# *.listを解析する
			validator/
				validator.py	# JSON Schemaを受け取って妥当性判断する
				schema/			# 構造・型・鍵・値の妥当性判定
					env.json	# env.yamlのスキーマ
					key.json	# key.yamlのスキーマ
	keymap/
		pc105-jp-OADG109A/		# 任意ディレクトリ
			SKB-SL18BKN/		# 任意ディレクトリ
				env.yaml		# デバイス識別子
				key.yaml		# メタキーマッピング
				24+shift.list	# 主要キーとその順序
				....list		# 戦略に応じて好きなように.listを作る
		other-keymap/
			other-device/
				env.yaml		# デバイス識別子
				key.yaml		# メタキーマッピング
				some.list		# 何かしらの設定
```

そもそもこれをどうコマンド引数で指定させるべきか。以下のような感じか？　なんて冗長なんだ。しかし他に術はない。ちゃんと視認できる名前にしようとするとこうなるし。相対パス設定をどこかでやってもいいかもしれないな。ぶっちゃけ`24+shift`という部分だけが重要。

```
kd.py --map pc105-jp-OADG109A/SKB-SL18BKN/24+shift.list
```

kd/config.yamlで設定するようにしようか。以下のように。

```yaml
args:
	map:
		prefix: "pc105-jp-OADG109A/SKB-SL18BKN/"
		suffix: ".list"
```

しかしこうなると`other-keymap/other-device/some.list`が指定できなくなってしまうわけだが。

例えば気分でキーボードを差し替えるユースケースにおいて、この設定では破綻する。どうしよう。

pc105-jp-OADG109A/SKB-SL18BKN/env.yaml
```yaml
human:
	device:
		model: "SKB-SL18BKN"
		name: "USBスリムキーボード"
os:
	device:
		name: "SIGMACHIP USB Keyboard"
	vender: 
		id: 0x1c4f
	product:
		id: 0x0027
key:
	map: "pc105-jp-OADG109A"
default: "24+shift.list"
```

ここでは`default: "24+shift.list"`のようにしてあるが。

そもそもデバイスやキーマップも指定しないといけない。そのデフォルト値はどこでどう決めるべき？　最終的にコマンド引数はどのように省略したり全部書いたりできるべき？　そのディレクトリ構造やコマンド記述パターンや解析方法はどうあるべき？　なんか、超大変そうだな。

もう面倒だから毎回コマンドで直接指定することにしようか。もし設定がなければ？デフォルトの`a-z0-9`にするか。

あと気がかりなのは、直接指定したら毎回設定データを妥当性確認しなきゃいけないことか。こう、YAMLや.listで記述したら自動的に妥当性確認し、成功ならバイナリファイルにして、人間が編集しづらい状態にし、そのファイルを指定することで「きっと妥当な値に違いない」と信用し、毎回妥当性確認をしないようにするのが良い気がしてきた。

つまり設定ファイルをコンパイルするイメージ。





